import { Lead, ServiceType } from '../../types';
import { NormalizedBusiness } from '../normalizer.service';
export declare function runIntelligence(business: NormalizedBusiness, service: ServiceType): Lead;
